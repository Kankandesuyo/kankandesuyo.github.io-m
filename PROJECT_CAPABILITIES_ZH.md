# Image2Model Studio 功能说明与优化方向

## 1. 项目定位

Image2Model Studio 是一个“二维图片生成三维模型”的静态前端 Demo。

当前版本重点展示产品交互流程和前端界面效果：用户上传一张二维图片，系统模拟 AI 生成三维模型的流程，最后加载本地示例 GLB 模型进行交互式预览。

第一版不真正运行 AI 模型，不调用真实生成接口，不包含后端服务。

## 2. 当前可以做什么

### 2.1 图片上传

当前支持用户上传本地图片：

- 支持点击上传
- 支持拖拽上传
- 支持 JPG、PNG、WEBP 格式
- 可以显示上传图片预览
- 可以显示文件名
- 可以显示文件大小
- 可以显示图片尺寸

这些功能全部在浏览器本地完成，不需要服务器。

### 2.2 生成流程模拟

点击 `Generate 3D Model` 后，页面会模拟一个 AI 生成流程。

当前包含 5 个步骤：

- Analyzing image
- Estimating depth
- Reconstructing mesh
- Generating texture
- Exporting GLB

每一步都有状态变化和进度动画，用于展示未来真实 AI 生成时的用户体验。

### 2.3 三维模型预览

生成流程完成后，页面会加载 `public/models` 目录中的示例 GLB 模型。

当前支持：

- 显示 GLB 三维模型
- 鼠标拖拽旋转
- 滚轮缩放
- 自动旋转开关
- 背景颜色切换
- 光照强度调节
- 全屏预览
- 在 Chair、Shoe、Toy 示例模型之间切换

### 2.4 模型下载

当前支持：

- 下载 GLB 示例模型

当前预留但尚未实现：

- OBJ 下载
- STL 下载

OBJ 和 STL 在页面中显示为 Coming Soon。

### 2.5 中英文切换

当前页面已经加入语言选择功能：

- English
- 中文

用户可以在页面顶部切换语言。切换语言只影响前端显示文案，不影响数据和模型文件。

### 2.6 GitHub Pages 部署

当前项目是纯静态前端项目，可以部署到 GitHub Pages。

已经包含：

- `vite.config.ts`
- GitHub Pages 子路径部署配置
- GitHub Actions workflow
- `npm run build` 输出 `dist`
- README 部署说明

## 3. 当前不能做什么

### 3.1 不能真正把图片生成 3D 模型

当前版本不会真正分析图片，也不会真正根据图片生成三维模型。

页面中的生成流程是前端模拟动画，最终展示的是本地预置的示例 GLB 模型。

也就是说，上传不同图片不会生成不同的真实三维模型。

### 3.2 不能调用真实 AI 模型

当前版本没有接入：

- TripoSR
- Stable Fast 3D
- Hunyuan3D
- TRELLIS
- 其他图生 3D API

这些模型只在页面中作为后续接入方向展示。

### 3.3 不能保存用户数据

当前版本没有数据库，所以不能保存：

- 用户上传历史
- 生成记录
- 模型文件记录
- 用户账号数据
- 项目列表

刷新页面后，本地上传状态会丢失。

### 3.4 不能处理大文件或复杂任务

当前版本只适合前端 Demo 展示。

它不具备后端文件处理能力，因此不能进行：

- 大文件上传分片
- 文件压缩
- 图片去背景
- 图片质量增强
- 模型格式转换
- 模型压缩
- 纹理烘焙
- 任务排队

### 3.5 不能安全保存 API Key

当前版本是静态前端，不能把真实 AI 服务的 API Key 写在代码里。

如果把 API Key 放进前端代码，部署后任何人都可以在浏览器中看到，这是不安全的。

真实接入 AI 服务时，必须通过后端或 Serverless 代理保存和调用 API Key。

## 4. 当前版本的价值

虽然当前版本不真正生成 3D 模型，但它已经可以用于展示完整产品原型。

它适合用于：

- 课程项目展示
- 产品概念 Demo
- 前端作品集
- GitHub Pages 在线展示
- 后续 AI 后端接入前的交互验证
- 向老师或评审说明产品流程

当前版本重点证明的是：

- 用户上传图片的入口可用
- 图生 3D 的生成流程清晰
- 3D 模型预览交互成立
- 下载入口和未来扩展方向明确
- 项目可以纯静态部署

## 5. 后续优化方向

### 5.1 接入真实 AI 生成能力

后续可以新增一个安全后端，用于调用真实图生 3D 模型。

推荐流程：

1. 前端上传图片到后端
2. 后端保存临时图片
3. 后端调用 TripoSR、Stable Fast 3D、Hunyuan3D 或 TRELLIS
4. 后端轮询生成状态
5. 后端返回生成后的 GLB 文件地址
6. 前端用真实 GLB 替换当前示例模型

这样可以保证 API Key 不暴露在浏览器中。

### 5.2 增加任务状态系统

真实 AI 生成通常需要等待一段时间。

后续可以加入：

- 任务 ID
- 排队状态
- 生成中状态
- 失败状态
- 重试按钮
- 生成耗时显示
- 生成历史记录

这样用户体验会更接近真实产品。

### 5.3 增强上传前处理

为了提高生成质量，可以在前端或后端增加图片预处理能力：

- 自动裁剪主体
- 自动去背景
- 图片压缩
- 图片尺寸检测
- 模糊图片提醒
- 低分辨率图片提醒
- 多视角图片上传

这些功能可以提高真实图生 3D 的成功率。

### 5.4 增强 3D 预览能力

当前预览功能已经可用，但还可以继续增强：

- 模型网格线开关
- 材质预览
- 纹理开关
- 坐标轴显示
- 模型尺寸测量
- 模型居中
- 截图导出
- 环境光贴图切换
- 移动端手势优化

这些功能会让项目更像专业 3D 工具。

### 5.5 增加更多导出格式

当前只支持 GLB 下载。

后续可以支持：

- OBJ
- STL
- FBX
- USDZ

其中：

- GLB 适合网页和实时预览
- OBJ 适合传统 3D 软件
- STL 适合 3D 打印
- USDZ 适合 Apple AR 场景

格式转换建议放在后端完成。

### 5.6 增加用户系统和作品库

如果项目从 Demo 升级为完整产品，可以加入：

- 用户登录
- 作品保存
- 生成历史
- 收藏模型
- 分享链接
- 删除模型
- 项目空间

这需要数据库和文件存储服务。

### 5.7 优化部署方案

当前版本适合 GitHub Pages。

如果后续加入真实 AI 后端，可以考虑：

- Vercel 前端 + Serverless API
- Cloudflare Pages + Workers
- Supabase 存储和数据库
- 自建 Node.js / Python 后端
- 对象存储保存生成模型

如果仍然只展示前端 Demo，GitHub Pages 已经足够。

## 6. 技术边界总结

当前版本是一个完整的静态前端交互 Demo。

它能展示产品流程、上传体验、生成动画、3D 模型预览和部署能力。

它不能真正进行 AI 生成，也不能安全地直接调用需要 API Key 的 AI 服务。

下一阶段的核心任务是增加安全后端，让前端从“模拟生成”升级为“真实生成”。

